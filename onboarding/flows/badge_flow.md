# Badge Flow

1. Verify petition status = open.  
2. Assign badge tier based on role and contribution.  
3. Log badge assignment in `chapter_resurrection.md`.  
4. Update `registry/badges.json` if new capabilities are added.  
5. Enforce badge gate via `badge_gate.bat` script.
